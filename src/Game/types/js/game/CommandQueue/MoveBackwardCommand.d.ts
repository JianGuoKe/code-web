export = MoveBackwardCommand;
declare class MoveBackwardCommand extends BaseCommand {
    constructor(gameController: any, highlightCallback: any, targetEntity: any);
}
import BaseCommand = require("./BaseCommand.js");
